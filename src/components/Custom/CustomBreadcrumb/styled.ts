import { styled, Button } from '@mui/material';

export const ButtonBreadcrumb = styled(Button)(() => `
    font-size: 24px; 
    text-transform: capitalize; 
    font-weight: bold;
    padding: 0;    
    margin: 0.8rem 6px 0.8rem 0;
`);