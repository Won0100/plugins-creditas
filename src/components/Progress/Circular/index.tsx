import * as MUI from '@mui/material';

export const ProgressCircular = () => {
    return (
        <MUI.Box textAlign="center" margin="45px 0">
            <MUI.CircularProgress />
        </MUI.Box>
    )
}