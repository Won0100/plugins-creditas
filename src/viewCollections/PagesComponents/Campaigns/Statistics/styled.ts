import { styled, Paper } from '@mui/material';

export const PaperContainer = styled(Paper)(() => ({
    width: '100%',
    padding: '20px',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
}));